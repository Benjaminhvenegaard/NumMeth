#include <math.h>

float fouser2(int n, float theta, float a[], float b[])
{
	float sinser(int, float, float []);
	float cosser(int, float, float []);

	return (cosser(n,theta,a)+sinser(n,theta,b));
}
